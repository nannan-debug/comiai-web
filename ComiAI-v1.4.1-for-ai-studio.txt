ComiAI 版本更新说明（给 AI Studio 同步）
版本号：v1.4.1
对比基线：v1.4.0（commit: 7a4bd78）
更新时间：2026-03-17

一、涉及文件
1) src/ScriptAndAssets.tsx
2) src/StoryboardManagement.tsx
3) src/StoryboardProduction.tsx
4) src/VideoPreview.tsx

二、核心改动概览
1) 全局视觉变量统一（对齐 test5.json）
- 新增并统一使用 UI Token：
  - primary: #01cd74
  - text: #1c2329
  - bg: #e8e9ea
  - border: #d2d3d4
  - window: #f8f8f9
  - subText: #a4a7a9
  - navBg: #1c2329e6
  - disabled: #dddddd
- 页面容器、卡片、工具栏、按钮状态从原先混合 Tailwind 色值迁移到同一套 Token 语义。

2) 剧本与设定页（ScriptAndAssets）结构重排
- 左侧顶部文案与入口调整：
  - 标题保持「本集剧本」
  - 右侧改为「导入剧本 / 手动编辑」
- 底部提示词区域改为“覆盖式大弹层”：
  - 点击「拆分镜提示词」后，弹层覆盖左侧剧本文本区域（视觉更聚焦）
  - 弹层内包含「生成方式」与对应「方案提示词」编辑区
- 生成方式极简化：
  - 仅保留两个选项标题：多参生视频 / 图生视频
  - 去除数字序号与小字备注
- 提示词标题极简化：
  - 去掉标题前数字，仅保留「多参生视频提示词 / 图生视频提示词」
- 右侧主区顶部改为分段切换条：
  - 「设定资产 / 拆分镜预览」统一在顶部 Segmented 控件切换
  - 去掉原大 Header 的复杂信息块
- 资产卡样式轻量化：
  - 卡片圆角、边框、占位新增卡改成简约风格
  - 栅格布局调整为更接近设计稿（含虚线“创建”占位）

3) 分镜管理页（StoryboardManagement）UI 同步
- 页面级容器/工具栏/侧栏统一为新 Token 体系
- 顶部工具条、模式标签、模型偏好按钮、批量执行按钮统一视觉
- 分镜卡内部统一：
  - 卡片底色/边框/分割线统一
  - 左侧序号栏与“进入精修”按钮样式统一
  - 图生/直出切换 Tab 统一为同一风格（激活态同主色）

4) 分镜制作页（StoryboardProduction）UI 同步
- 全局背景、侧栏、底部栏、历史记录面板统一 Token
- 主按钮（生成、查看预览）统一深底+主色强调
- 选中态边框/标签统一主色高亮

5) 视频预览页（VideoPreview）UI 同步
- 中央播放器、左右侧栏、底部时间轴统一 Token
- 粗剪/审阅模式切换、导出按钮、版本菜单统一样式
- 右侧审阅折叠态与计数状态统一主色语义
- 遮罩层颜色改为 Token 变量（mask）

三、与上版（v1.4.0）代码层差异统计
- 变更文件数：4
- 总体变更：249 insertions / 315 deletions
- 说明：主要是样式重构与布局重排，业务数据结构和核心交互流程保持兼容。

四、AI Studio 同步建议
1) 先覆盖以上 4 个文件
2) 重点检查页面：
   - Step1 剧本与设定页：左侧提示词弹层覆盖行为 + 右侧顶部双 Tab
   - Step2 分镜管理页：分镜卡工具条和图生/直出切换样式
   - Step3/Step4：确认 Token 色值在按钮、边框、背景的一致性
3) 若 AI Studio 环境样式缓存未刷新，请清缓存后重载。

