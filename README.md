# ComiAI

当前版本：`v2.0.2`

## 更新说明
本次版本按项目发布规范重新整理并发布：

### 1. 发布规范恢复
- 补回 `RELEASE.md`，恢复版本命名、Tag 命名、Release 标题与发布清单规范。
- 明确后续发布使用语义化版本 `vMAJOR.MINOR.PATCH`。

### 2. 版本信息统一
- 将 `package.json` / `package-lock.json` 版本统一更新为 `2.0.2`。
- 项目包名统一为 `comiai-web`，与仓库命名保持一致。

### 3. README 结构规范化
- 标题固定为 `# ComiAI`。
- 版本号改为标题下单独展示，不再写入标题行。

## 如何运行
1. 安装依赖：`npm install`
2. 启动开发服务器：`npm run dev`
3. 构建项目：`npm run build`
